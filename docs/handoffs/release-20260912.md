# 2026-09-12 发布记录：数据库迁移与新版上线

> 历史交接：正文进度、待办、分支与验证结果只代表记录时点；当前进度统一维护在 [STATUS.md](../STATUS.md)。操作步骤见 [操作手册索引](../../README.md#操作手册)，只需定位模块时见 [任务与代码索引](../README.md)。

## 结论

新版（`main` 的 `4607e6e`，PR #25）已上线。生产 deployment 为 `dpl_9i1xfZpdyp9fcJ648RMbLTdnZ5Zu`。上线内容包括平台管理、管理员跨校测试、好友与私聊、可靠发送、举报，以及此前已有的课程搜索。

- **前端回滚目标**：`dpl_GRDM8T2uzj4VqswkCnHT1JVfochf`（提交 `2b3cb4e`）。回滚前端不会回滚数据库。迁移只新增对象、重写函数，旧前端用到的函数都还在，预期兼容，但**没有实测**。
- 数据库**没有反向迁移**。按[部署手册](../runbooks/deploy.md)，出问题先恢复旧前端，不要删数据或盲目反向迁移。

## 执行过程与证据

1. **只读核对**：生产结构与[发布盘点](./release-preflight-20260912.md)一致（20 张表、33 个 public 函数）。盘点之后导入过课程：课程目录与当前学期课程各 24 门。成员 4 人、资料 3 份，消息与好友关系均为 0。
2. **备份**：生产是免费版，平台备份列表为空。执行前把 public 下 20 张表全部导出成 JSON，存到仓库外的 `D:/Projects/self_Project/CourseMate-backups/`。auth 用户只存了 ID，不含邮箱。迁移不改 auth schema。
3. **演练**：在 PGlite 里按生产现状重建（前 11 条迁移，外加生产上额外开启的两张限流表 RLS），载入导出的数据，再执行 9 条迁移。结果：全部成功；原有各表行数不变；每门课都有课程会话；4 位成员迁移后的当前学校与可见课程都正确。
4. **执行前探针**：经 `supabase db query --linked` 执行多语句显式事务，可以整体执行；执行角色是 `postgres`，与现有函数、表的属主一致。故意让一个事务失败后，其中建的表不存在，证明**失败会整体回滚**。旧 SQL Editor 那种半途执行的事故不会重演。
5. **执行**：`202609100006` 第一次执行成功。因为脚本的文件清单写重了，同一文件被第二次执行，报 `42P07`（表已存在）后整体回滚，没有副作用。确认状态后，按明确清单执行其余 8 条，全部成功。`202609110001` 和 `202609110006` 本身不带 `begin/commit`，执行时在外面包了一层事务。
6. **执行后核对**：
   - 表 29 张（新增 9 张）。
   - 未登录只能执行 `enabled_school_id_for_email_domain`。
   - `friend_rate_limit_config` 与 `friend_rate_limit_buckets` 的 RLS 保留。
   - Realtime publication 仍包含 `messages`。
   - 课程、目录、会话、成员、资料的行数不变。
7. **迁移历史**：`supabase migration repair --status applied` 建立了 `supabase_migrations.schema_migrations`，登记全部 20 条。今后 `db push --dry-run` 能正确判断缺哪些迁移。
8. **类型**：从生产重新生成 `src/types/database.ts`，与本地完整迁移链对比，表和函数集合一致。生产多出的 `admin_school_test_context` 与 `effective_member_school_id` 正来自 `202609120002`，本地旧类型当时没有收录。其余差异只是生成器写法。
9. **代码**：PR #25 合并进 `main`（`4607e6e`）。依次运行 `npm test`（73 个文件、444 项）→ build → lint → typecheck，全部通过。
10. **部署**：从干净的 `main` 手动 `npx vercel deploy --prod`。`vercel.json` 对 `main` 的自动部署暂停**没有改动**。
11. **冒烟（未登录）**：`/login` 返回 200，列出两所学校；`/admin`、`/dashboard`、`/friends`、`/profile` 都 307 跳转登录并带上 `next`；页面上没有报错。

## 仍待完成

- **指定所有者**：管理页要先有所有者才能用，见[管理手册](../runbooks/platform-admin.md)「指定所有者」。
- **登录后的完整验收（Issue #19）**：onboarding、管理页、跨校切换、课程群聊、好友、私聊发送与重试、拉黑、举报、Realtime。
- 是否恢复 `main` 的 Git 自动部署。
- 补一条迁移，声明两张限流表的 RLS，使本地迁移链与线上一致。
- 完整课表的预演与导入。
- 私聊物理清理调度保持关闭，没有启用。
