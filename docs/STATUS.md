# 当前状态

整理日期：2026-09-10。代码基线：本地 `main` 的 `4814716`。
这是项目进度的统一摘要；线上信息转录自交接记录，本次仅核对本地代码，未重新验证线上环境或运行测试。

## 本地已实现

| 模块 | 范围 |
|---|---|
| 认证 | 开放学校的精确邮箱域名、六位 OTP、成员账号绑定、会话与当前设备退出 |
| 个人资料 | 首次登录必须补全显示名称，之后可在 `/profile` 修改 |
| 课程 | 大厅搜索、加入退出、课程群聊、历史分页、Realtime 与断线补拉、旧学期归档 |
| 好友后端 | 搜索成员、好友申请、备注、删除、拉黑、接受申请时创建或复用私聊 |
| 平台管理 | `/admin`、所有者与管理员角色、学校与域名、课表导入、学期切换、审计记录 |

好友页面（#15）和完整私聊消息功能（#16）尚未交付；课程页已有“添加好友”链接，不代表目标页面已实现。
学习搭子、笔记共享尚未开发，笔记上线前必须先有举报与内容审核。

## 线上待核实与下一步

以[环境交接](handoffs/environment-and-deployment.md)和[管理页交接](handoffs/platform-admin.md)最后记录为起点：

- 前 11 个 migration 已应用；第 12 个 `202609100006_platform_admin.sql` 待执行，所有者待指定。管理页代码已部署，未启用时无入口、访问显示 404。
- 应用管理 migration 后，重新生成 `src/types/database.ts`；当前管理函数类型为临时手工补充。
- 课程目录记录为空，待导入并生成当前学期课程；先在管理页或导入脚本中预演。
- Vercel Git 集成记录为未连接，推送不会自动上线；操作前按[部署手册](runbooks/deploy.md)核实。
- 真实验证码边界、重发冷却、会话保持、多设备退出，以及登录后的完整业务与管理流程仍有验收项。认证进度以 GitHub Issue #7 最新记录为准。
- 分支保护与 Code Owners 强制审核记录为尚未启用；协作者 Supabase 后台权限的安排仍需协调。
- 试点学校、产品定位与双语方案仍待决定；现有两校配置是测试配置。

既有验证记录：管理页交接记载 320 项测试及构建、lint、类型检查通过；该数字是历史结果，不代表当前再次验证。

## 如何更新

功能交付、待办完成或环境核实后，只更新这里对应的状态、日期和证据入口；不要在各份交接里重复维护“最新进度”。
稳定规则放 `CONTEXT.md` / ADR，操作步骤放 runbook，事故细节与当时证据保留在 handoff。
文档与实际代码或环境不一致时，明确记录差异，完成核实后再更新；不能把旧记录当作新的测试结果。
