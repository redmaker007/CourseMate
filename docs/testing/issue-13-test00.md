# Issue #13：TEST00 联调课程

这组数据只用于手动体验 Issue #13 的真实课程流程，不属于正式课程目录，也不会随 schema migration 或产品代码自动写入。

## 固定记录

| 学校 | 课程 ID | 代码 | 名称 | 学期 |
| --- | --- | --- | --- | --- |
| `uw-madison` | `13000000-0000-4000-8000-000000000001` | `TEST00` | `测试00-测试课程` | `2026-fall` |
| `umich` | `13000000-0000-4000-8000-000000000002` | `TEST00` | `测试00-测试课程` | `2026-fall` |

## 应用与核验

1. 先确认目标 Supabase 项目；未经确认不得向托管数据库执行。
2. 先应用 Issue #13 migration，再手动执行 `supabase/seeds/issue-13-test00.sql`。脚本可重复执行；若目标位置已有非本 fixture 的 TEST00，会主动失败而不是覆盖。
3. 写入后查询并记录两行的实际 `created_at`。当前仓库没有保存伪造的创建时间，因为本次实现没有连接或写入托管数据库。
4. 为两校各准备一个已完成 onboarding 的测试成员，按正常产品流程搜索、加入、聊天、查看成员和退出。

## 正式上线前清理

再次确认目标项目后，执行 `supabase/seeds/issue-13-test00-cleanup.sql`。清理只按上表的固定 UUID 和完整身份字段删除；字段被修改时脚本会拒绝删除。课程拥有的会话、成员关系和消息会随课程一并清理，不会留下不可达会话。

正式环境的课程与学期数据应由后续课程数据源提供，不能把 TEST00 seed 接入自动部署、正式 seed 或产品条件分支。
